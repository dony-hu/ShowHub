import React from 'react';
import './BugReportsSection.css';

interface BugReportsSectionProps {
  onBack: () => void;
}

interface BugReport {
  id: number;
  title: string;
  description: string;
  detailedDescription?: string;
  severity: 'high' | 'medium' | 'low';
  status: 'open' | 'in-progress' | 'resolved';
  date: string;
  type: string;
  apiName?: string;
  referenceLinks?: Array<{
    label: string;
    url: string;
  }>;
  affectedScenarios?: string[];
  suggestion?: string;
}

const BugReportsSection: React.FC<BugReportsSectionProps> = ({ onBack }) => {
  const bugReports: BugReport[] = [
    {
      id: 1,
      title: '14+个API文档页面链接失效',
      description: '地理编码、逆地理编码等多个核心API文档返回"无此产品或服务"',
      detailedDescription: `经测试发现以下API文档页面访问失效：
• /develop/7（地址输入提示）返回404
• /develop/8（地理编码）返回"无此产品"  
• /develop/9（逆地理编码）返回"无此产品"
• /develop/10（地址真实性校验）返回"无此产品"

这导致开发者无法访问完整的API参数说明、返回示例、使用指南。`,
      type: '文档问题',
      severity: 'high',
      status: 'open',
      date: '2025-01-24',
      apiName: '多个API',
      referenceLinks: [
        { label: '四级行政区划查询（可访问）', url: 'https://lbs.sfmap.com.cn/develop/12' },
        { label: '地址类型识别（可访问）', url: 'https://lbs.sfmap.com.cn/develop/17' }
      ],
      affectedScenarios: ['新开发者入门', '接口参数查询', '错误排查'],
      suggestion: '立即恢复所有API文档链接，建立链接检查机制'
    },
    {
      id: 2,
      title: '参数文档与实际逻辑不一致',
      description: '参数表标记为"必填"，但接口说明又允许参数可选',
      detailedDescription: `参数表显示province、city、county都是必填
但接口说明中写道：
"如果参数都不传，返回所有省"
"只传province，返回该省下所有市"

这自相矛盾：如果是必填，怎么能都不传？
缺少参数组合规则说明。`,
      type: '文档问题',
      severity: 'high',
      status: 'open',
      date: '2025-01-24',
      apiName: '四级行政区划查询',
      referenceLinks: [
        { label: '问题页面', url: 'https://lbs.sfmap.com.cn/develop/12' }
      ],
      affectedScenarios: ['参数验证', '请求失败排查', '开发者困惑'],
      suggestion: '添加参数组合规则章节，明确哪些参数是可选的'
    },
    {
      id: 3,
      title: '返回参数类型说明与示例不符',
      description: '返回参数表显示data字段类型为object，但示例中data是数组',
      detailedDescription: `返回参数表：
| data | 响应数据集 | object |

返回示例：
{
  "result": {
    "data": [
      "南头街道",
      "南山街道"
    ]
  }
}

明显矛盾：表格说data是object，示例显示data是string[]`,
      type: '文档问题',
      severity: 'high',
      status: 'open',
      date: '2025-01-24',
      apiName: '四级行政区划查询',
      referenceLinks: [
        { label: '返回参数表', url: 'https://lbs.sfmap.com.cn/develop/12#返回参数' }
      ],
      affectedScenarios: ['数据解析', '类型定义', '前端集成'],
      suggestion: '更正返回参数表：data应标记为array<string>类型'
    },
    {
      id: 4,
      title: '缺少完整的错误码说明表',
      description: '缺少status=1时的错误返回示例和错误信息字段说明',
      detailedDescription: `当前所有API文档只展示成功场景（status=0）
缺少以下错误场景：
1. 无效参数错误 - 返回什么？
2. 权限错误 - 如何区分？
3. 限流错误 - HTTP状态码和返回体是什么？
4. 无数据场景 - 返回空数组还是错误？
5. 服务内部错误 - 如何区分是否可重试？
6. 超时场景 - 客户端如何检测？

导致开发者无法进行完整的错误处理。`,
      type: '文档问题',
      severity: 'high',
      status: 'open',
      date: '2025-01-24',
      apiName: '全部API',
      referenceLinks: [
        { label: 'HTTP状态码规范', url: 'https://httpwg.org/specs/rfc7231.html#status.codes' }
      ],
      affectedScenarios: ['错误处理', '故障排查', '用户反馈'],
      suggestion: '发布API错误码完整参考文档，列出所有可能的失败状态'
    },
    {
      id: 5,
      title: '地址类型识别API参数说明模糊',
      description: 'opt参数说明不清，缺少参数值为poi时的说明',
      detailedDescription: `opt参数文档说明：
"参数为空，输出poi+aoi混合类型 参数为aoi，输出aoi类型"

问题：
1. 语句不清楚，应该用表格
2. 缺少opt="poi"的说明
3. 缺少参数示例和默认行为
4. 没有说明参数大小写敏感性
5. 无效参数值时的处理`,
      type: '文档问题',
      severity: 'medium',
      status: 'open',
      date: '2025-01-24',
      apiName: '地址类型识别',
      referenceLinks: [
        { label: '问题API文档', url: 'https://lbs.sfmap.com.cn/develop/17' }
      ],
      affectedScenarios: ['参数组合', '请求失败', '功能实现'],
      suggestion: '使用表格重新组织参数说明，列出所有参数取值及对应行为'
    },
    {
      id: 6,
      title: '缺少数据字段值的对照表',
      description: 'typecode字段值没有对应的编码说明',
      detailedDescription: `返回示例：
{
  "typecode": "190403",
  "mediumname": "门牌信息",
  "smallname": "楼栋号"
}

问题：
1. typecode "190403"代表什么？
2. 如何解析？每位数字的含义？
3. 缺少typecode对照表
4. mediumname和smallname的层级关系不清
5. 是否所有地址都能被准确分类？

导致开发者需要逐个测试才能理解数据含义。`,
      type: '文档问题',
      severity: 'high',
      status: 'open',
      date: '2025-01-24',
      apiName: '地址类型识别',
      referenceLinks: [
        { label: '返回参数说明', url: 'https://lbs.sfmap.com.cn/develop/17#返回参数' }
      ],
      affectedScenarios: ['数据分类', '业务逻辑', '结果展示'],
      suggestion: '发布地址类型编码对照表和类型层级关系文档'
    },
    {
      id: 7,
      title: '缺少API调用限制说明',
      description: '无QPS限制、日均额度限制、单次请求大小限制说明',
      detailedDescription: `开发者需要了解但文档完全缺失：

1. 请求频率限制
   - 单账户QPS是多少？
   - 是否有突发流量处理？

2. 日均/月均额度
   - 免费层日均调用次数？
   - 商用版有无限制？

3. 单次请求限制
   - 最大POST body大小？
   - 最多支持多少条地址批量？

4. 超时设置
   - 服务端响应超时时间？
   - 是否支持请求级别超时？

5. 并发限制
   - 单用户最大并发连接数？
   - 单IP限制？

缺乏这些信息导致无法评估成本、架构设计无法优化。`,
      type: '文档问题',
      severity: 'high',
      status: 'open',
      date: '2025-01-24',
      apiName: '全部API',
      referenceLinks: [
        { label: '高德配额说明', url: 'https://lbs.amap.com/api/webservice/geopositioning/' }
      ],
      affectedScenarios: ['架构设计', '成本评估', '容量规划'],
      suggestion: '发布API调用限制与配额说明文档'
    },
    {
      id: 8,
      title: '缺少坐标系说明',
      description: '地理编码API应说明返回坐标使用的坐标系',
      detailedDescription: `坐标系问题的重要性：

1. 不同坐标系精度差异很大
   - WGS-84（全球标准）
   - GCJ-02（中国火星坐标）
   - 同一个点的经纬度可能相差几百米

2. 错误的坐标系使用导致
   - 地点定位偏差
   - 路线规划错误
   - 与其他地图服务不兼容

应该说明：
- 返回坐标系标准
- 是否支持坐标系转换
- 与地图展示组件的匹配关系`,
      type: '文档问题',
      severity: 'medium',
      status: 'open',
      date: '2025-01-24',
      apiName: '地理编码',
      referenceLinks: [
        { label: '高德坐标系说明', url: 'https://lbs.amap.com/api/webservice/geopositioning' }
      ],
      affectedScenarios: ['地点定位', '地图展示', '跨系统集成'],
      suggestion: '补充坐标系与精度说明章节'
    },
    {
      id: 9,
      title: '缺少批量处理接口或说明',
      description: '大批量数据需要循环调用单条接口',
      detailedDescription: `实际业务场景：
- 地址校验：一次需要处理10,000+条地址
- 企业查询：日均查询100万级企业信息
- 地理编码：需要给100万商户进行坐标转换

当前问题：
1. 没有批量API
   - 必须循环单条请求
   - 1000条地址需要1000次请求
   - 网络往返延迟×1000

2. 性能无法优化
   - 无缓存建议
   - 无并发调用指南

3. 成本无法评估
   - 如果有最小批量量？
   - 批量与单条的价格是否不同？`,
      type: '文档问题',
      severity: 'medium',
      status: 'open',
      date: '2025-01-24',
      apiName: '地址相关API',
      affectedScenarios: ['大数据处理', '导入导出', '定期同步'],
      suggestion: '补充批量处理指南，或提供服务端批处理API'
    },
    {
      id: 10,
      title: '缺少最佳实践和常见错误指南',
      description: '缺少最佳实践指南、常见错误排查、性能优化建议',
      detailedDescription: `新开发者面临的问题：
- 参数怎样设置性能最优？
- 返回的数据如何缓存？
- 如何处理高并发场景？
- 请求失败应该重试吗？
- 如何监控API性能？

常见错误处理：
- 不知道某些参数会显著影响响应时间
- 错误的字段类型导致数据解析失败
- 没有错误重试机制
- 同步阻塞等待所有请求
- 没有超时保护

应该包含的内容：
1. 参数优化建议
2. 错误处理方案
3. 并发处理指南
4. 性能监控方法`,
      type: '文档问题',
      severity: 'medium',
      status: 'open',
      date: '2025-01-24',
      apiName: '全部API',
      referenceLinks: [
        { label: 'Google API最佳实践', url: 'https://cloud.google.com/docs/authentication/best-practices' }
      ],
      affectedScenarios: ['生产环境', '性能优化', '故障排查'],
      suggestion: '发布API最佳实践指南，包含常见错误案例和解决方案'
    },
    {
      id: 11,
      title: '缺少多编程语言代码示例',
      description: '所有示例仅提供JSON格式，缺少主流语言实现',
      detailedDescription: `当前文档代码示例问题：

1. 仅有JSON示例
   - 无法直接复制使用
   - 开发者需要自己实现HTTP请求逻辑
   - 容易出现编码、超时、异常处理错误

2. 缺少主流语言示例
   - Python（数据分析最常用）
   - Java（企业应用标准）
   - JavaScript/Node.js（前端集成）
   - Go（性能型应用）
   - C#/.NET（Windows应用）

3. 缺少SDK
   - 是否有官方SDK？
   - 是否支持流行的HTTP库？

开发者成本：
- 集成时间增加2-3倍
- 测试覆盖不完整
- 容易引入安全问题`,
      type: '文档问题',
      severity: 'medium',
      status: 'open',
      date: '2025-01-24',
      apiName: '全部API',
      affectedScenarios: ['快速集成', '开发效率', '错误降低'],
      suggestion: '提供Python、Java、JavaScript等主流语言的完整代码示例'
    },
    {
      id: 12,
      title: '缺少参数可选组合的说明',
      description: '某些API的参数在不同情况下的必填性不同',
      detailedDescription: `参数组合问题（以四级行政区划查询为例）：

场景1：查询全国所有省
不传任何地区参数
→ 返回所有省

场景2：查询广东省下的市
只传province=广东省
→ 返回该省下所有市

场景3：查询深圳市下的区
传province+city
→ 返回该市下所有区

场景4：查询南山区下的街道
传province+city+county
→ 返回该区下所有街道

场景5：有效但文档未说明
- 可以只传city吗？
- 可以只传county吗？
- province+county（跳过city）可以吗？

缺乏清晰的参数组合规则导致：
- 开发者尝试测试失败的组合
- 无法准确构建查询逻辑`,
      type: '文档问题',
      severity: 'medium',
      status: 'open',
      date: '2025-01-24',
      apiName: '四级行政区划查询等',
      referenceLinks: [
        { label: '参数组合规则', url: 'https://lbs.sfmap.com.cn/develop/12' }
      ],
      affectedScenarios: ['参数设计', '场景探索', '集成测试'],
      suggestion: '添加参数组合规则表，列出所有有效的参数组合'
    },
    {
      id: 13,
      title: '缺少数据准确性说明',
      description: '文档声称精准度99.81%，但无详细定义和测试范围',
      detailedDescription: `丰图官网宣称的数据指标：
"语义准确度达99.81%"
"地址解析准确率100%"

但这些数据的真实含义不清：

1. 精准度的定义
   - 是字符级匹配准确率？
   - 还是地理位置准确度？
   - 误差范围多少？

2. 测试的数据范围
   - 测试了多少条地址？
   - 覆盖了全国所有地区吗？
   - 包括偏远农村地区吗？

3. 不同地区的准确度
   - 城市与农村的准确度差异？
   - 数据多久更新一次？
   - 新地址何时能被识别？

对开发者的影响：
- 无法判断API是否适合自己的业务
- 无法评估需要多少人工核验`,
      type: '文档问题',
      severity: 'medium',
      status: 'open',
      date: '2025-01-24',
      apiName: '地址输入提示',
      referenceLinks: [
        { label: '官网数据声称', url: 'https://lbs.sfmap.com.cn/' }
      ],
      affectedScenarios: ['质量评估', '选型决策', 'SLA设定'],
      suggestion: '补充数据准确度说明文档，明确定义、测试方法、地区差异'
    },
    {
      id: 14,
      title: '缺少API响应时间SLA说明',
      description: '无平均响应时间、P99响应时间等性能指标',
      detailedDescription: `生产环境中需要了解的性能指标：

1. 响应时间
   - P50响应时间（中位数）？
   - P95响应时间（95%分位数）？
   - P99响应时间（99%分位数）？

2. 超时配置
   - 推荐的请求超时时间？
   - 服务端会在多久后强制断开？

3. 可用性
   - 服务可用性SLA是多少？
   - 是否有服务级别保证？

4. 性能规律
   - 不同时段的性能差异？
   - 并发请求下的响应时间？

实际应用影响：
- 无法设置合理的超时时间
- 无法承诺给客户的响应时间
- 无法设计降级方案`,
      type: '文档问题',
      severity: 'medium',
      status: 'open',
      date: '2025-01-24',
      apiName: '全部API',
      referenceLinks: [
        { label: 'AWS SLA声明', url: 'https://aws.amazon.com/cn/about-aws/global-infrastructure/regional-product-services/' }
      ],
      affectedScenarios: ['性能评估', 'SLA承诺', '超时配置'],
      suggestion: '发布性能指标说明文档，包含响应时间基准和SLA'
    },
    {
      id: 15,
      title: '缺少字段排序规则说明',
      description: '返回数据集中的数据顺序规则不清楚',
      detailedDescription: `在四级行政区划查询返回的街道列表中：
"南头街道", "南山街道", "沙河街道", ...

问题：
1. 这个顺序是按什么排序的？
   - 按行政区划代码？
   - 按名称拼音？
   - 按面积大小？

2. 排序是否稳定？
   - 每次请求返回的顺序相同吗？
   - 如果数据更新，顺序会变吗？

3. 分页问题
   - 数据量大时如何分页？
   - 是否支持limit/offset？

实际影响：
- 前端展示时不知道如何排序
- 用户体验差`,
      type: '文档问题',
      severity: 'low',
      status: 'open',
      date: '2025-01-24',
      apiName: '四级行政区划查询',
      referenceLinks: [
        { label: '返回示例', url: 'https://lbs.sfmap.com.cn/develop/12#返回示例' }
      ],
      affectedScenarios: ['数据展示', '用户体验', '排序逻辑'],
      suggestion: '补充排序规则说明，如支持排序参数则详细说明'
    },
    {
      id: 16,
      title: '缺少数据更新频率说明',
      description: '地址数据的更新频率不明确',
      detailedDescription: `对数据及时性有影响的关键问题：

1. 数据更新频率
   - 行政区划数据多久更新一次？
   - 企业信息数据多久更新一次？
   - 地名地址数据多久更新一次？

2. 新增数据的可见性
   - 新成立的企业多久能被查询到？
   - 新开楼盘多久能被地理编码？
   - 地址变更多久能被识别？

3. 数据一致性
   - 多个数据源之间是否一致？
   - 行政区划变更时如何同步？

4. 历史数据
   - 是否支持查询历史版本的数据？
   - 是否能追溯数据的修改记录？

实际应用影响：
- 实时数据应用无法准确承诺
- 数据导入导出的合理性无法评估
- 周期性更新的时机无法确定`,
      type: '文档问题',
      severity: 'medium',
      status: 'open',
      date: '2025-01-24',
      apiName: '全部地址API',
      affectedScenarios: ['数据同步', '实时性保证', '周期规划'],
      suggestion: '补充数据更新政策说明，明确各类数据的更新频率'
    },
    {
      id: 17,
      title: '缺少地域覆盖范围说明',
      description: '各API的数据覆盖范围不清楚',
      detailedDescription: `不同API的数据覆盖范围可能不同，但文档未说明：

1. 地理范围
   - 仅覆盖中国还是全球？
   - 中国境内是否全覆盖？
   - 香港、澳门、台湾是否包含？

2. 数据覆盖度
   - 城市覆盖率多少？
   - 县城、乡镇的覆盖率？
   - 偏远村庄是否覆盖？

3. 地址类型覆盖
   - 所有类型地址都支持吗？
   - 门牌号、楼栋号、室号覆盖度？
   - 虚拟地址、临时地址？

4. 与地图、定位的关系
   - 地址库与地图展示区域是否一致？
   - 定位API的覆盖范围？
   - 室内定位的覆盖建筑物？

无法准确评估产品适用范围`,
      type: '文档问题',
      severity: 'medium',
      status: 'open',
      date: '2025-01-24',
      apiName: '全部API',
      referenceLinks: [
        { label: '覆盖范围说明', url: 'https://lbs.sfmap.com.cn/' }
      ],
      affectedScenarios: ['产品适用范围', '业务扩展', '质量评估'],
      suggestion: '发布数据覆盖范围说明文档，包含地理范围地图'
    },
    {
      id: 18,
      title: '缺少废弃API的迁移指南',
      description: '若某个API版本被弃用，应提供迁移指南',
      detailedDescription: `在API演进过程中，旧版本可能被标记为废弃。

应该清楚说明：

1. 弃用周期
   - 何时开始停止维护？
   - 何时完全下线？
   - 具体时间表是什么？

2. 迁移指南
   - 推荐的替代API是什么？
   - 新旧API的功能对比？
   - 参数如何映射？
   - 返回数据如何转换？

3. 迁移工具
   - 是否提供自动转换脚本？
   - 是否提供迁移检查工具？

4. 支持期限
   - 旧版本API的支持截止日期？
   - 截止后是否直接返回错误？

缺乏迁移指导导致：
- 开发者可能使用已废弃的API
- 突然下线导致服务故障`,
      type: '文档问题',
      severity: 'low',
      status: 'open',
      date: '2025-01-24',
      apiName: '全部API',
      referenceLinks: [
        { label: 'Google API弃用政策', url: 'https://cloud.google.com/apis/design/versioning' }
      ],
      affectedScenarios: ['长期维护', '版本升级', '成本管理'],
      suggestion: '建立API生命周期管理政策'
    },
    {
      id: 19,
      title: '文档导航和索引不完整',
      description: '缺少API总览页、功能分类导航',
      detailedDescription: `当前文档的导航问题：

1. 缺少统一入口
   - 无API总览页
   - 无完整的API列表
   - 无功能分类导航
   - 搜索功能不便捷

2. 查找困难
   - 有多少个API？
   - 它们分别用来做什么？
   - 按功能分类是怎样的？
   - 推荐的使用流程？

3. 链接管理
   - API编号规律？
   - 如何发现新增API？
   - 旧API与新API的关系？

用户体验差的表现：
- 新用户不知道从哪里开始
- 无法快速定位需要的API
- 文档间的跳转不顺畅
- 无法发现相关的补充API`,
      type: '文档问题',
      severity: 'medium',
      status: 'open',
      date: '2025-01-24',
      apiName: '全部',
      referenceLinks: [
        { label: '高德API导航', url: 'https://lbs.amap.com/api/webservice/guide/api/georegeo' }
      ],
      affectedScenarios: ['开发者入门', '功能探索', '产品发现'],
      suggestion: '重新设计API文档导航，提供按功能分类的总览'
    },
    {
      id: 20,
      title: '返回数据示例不包含错误场景',
      description: '所有文档示例都仅展示成功场景',
      detailedDescription: `当前文档的问题：

1. 仅有成功示例
   - 返回status=0的示例
   - 缺少status=1的示例
   - 缺少各种错误场景的返回

2. 常见错误场景缺失
   - 无效参数错误
   - 权限错误
   - 限流错误
   - 无数据场景
   - 服务内部错误
   - 超时场景

3. 错误字段不清楚
   - 错误信息在哪个字段？
   - 是否有错误码字段？
   - 嵌套结构如何？

4. 多语言错误
   - 错误信息是中文还是英文？
   - 是否支持多语言？

5. 开发者体验
   - 需要依赖实际调用才能知道错误
   - 容易遗漏某些错误处理
   - 自动化测试困难`,
      type: '文档问题',
      severity: 'medium',
      status: 'open',
      date: '2025-01-24',
      apiName: '全部API',
      referenceLinks: [
        { label: 'HTTP状态码规范', url: 'https://httpwg.org/specs/rfc7231.html#status.codes' }
      ],
      affectedScenarios: ['错误处理', '用户体验', '故障诊断'],
      suggestion: '在每个API文档补充错误场景部分，提供完整的返回示例'
    }
  ];

  const getSeverityLabel = (severity: string) => {
    const labels: { [key: string]: string } = {
      high: '严重',
      medium: '中等',
      low: '轻微'
    };
    return labels[severity] || severity;
  };

  const getStatusLabel = (status: string) => {
    const labels: { [key: string]: string } = {
      open: '待处理',
      'in-progress': '处理中',
      resolved: '已解决'
    };
    return labels[status] || status;
  };

  return (
    <div className="bug-reports-section">
      <button className="back-button" onClick={onBack}>
        ← 返回
      </button>
      
      <div className="section-header">
        <h2>产品缺陷</h2>
        <p className="section-subtitle">丰图开放平台 API 文档问题分析（20项）</p>
      </div>

      <div className="bugs-list">
        {bugReports.map((bug) => (
          <div key={bug.id} className="bug-item">
            <div className="bug-header">
              <h3>{bug.title}</h3>
              <div className="bug-badges">
                <span className="type-badge">{bug.type}</span>
                <span className={`severity-badge ${bug.severity}`}>
                  {getSeverityLabel(bug.severity)}
                </span>
                <span className={`status-badge ${bug.status}`}>
                  {getStatusLabel(bug.status)}
                </span>
              </div>
            </div>
            <p className="bug-description">{bug.description}</p>
            {bug.detailedDescription && (
              <div className="bug-detailed-description">
                <p className="detailed-label">📝 详细说明：</p>
                <p className="detailed-text">{bug.detailedDescription}</p>
              </div>
            )}
            <div className="bug-meta">
              {bug.apiName && <span className="api-name">📌 涉及API：{bug.apiName}</span>}
            </div>
            {bug.affectedScenarios && bug.affectedScenarios.length > 0 && (
              <div className="affected-scenarios">
                <span className="scenarios-label">⚠️ 影响场景：</span>
                <div className="scenarios-list">
                  {bug.affectedScenarios.map((scenario, idx) => (
                    <span key={idx} className="scenario-tag">{scenario}</span>
                  ))}
                </div>
              </div>
            )}
            {bug.referenceLinks && bug.referenceLinks.length > 0 && (
              <div className="reference-links">
                <span className="links-label">🔗 参考链接：</span>
                <div className="links-list">
                  {bug.referenceLinks.map((link, idx) => (
                    <a 
                      key={idx} 
                      href={link.url} 
                      target="_blank" 
                      rel="noopener noreferrer"
                      className="reference-link"
                    >
                      {link.label} →
                    </a>
                  ))}
                </div>
              </div>
            )}
            {bug.suggestion && (
              <div className="suggestion-box">
                <p className="suggestion-label">💡 改进建议：</p>
                <p className="suggestion-text">{bug.suggestion}</p>
              </div>
            )}
            <div className="bug-footer">
              <span className="bug-date">{bug.date}</span>
            </div>
          </div>
        ))}
      </div>
    </div>
  );
};

export default BugReportsSection;
